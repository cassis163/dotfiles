- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bashrc:
    ~/.bash_logout:
    ~/.gitconfig:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
